# i18n 国际化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 fi-code 的 Web 前端、TUI 和后端引入完整的 i18n 国际化体系，支持中文/英文自动检测和手动切换。

**Architecture:** Web 使用 react-i18next，TUI 和后端使用 rust-i18n，通过统一键名规范保持一致性。

**Tech Stack:** React (i18next), Rust (rust-i18n), Axum (X-Lang Header)

---

## 文件结构

| 文件 | 动作 | 职责 |
|------|------|------|
| `frontend/package.json` | 修改 | 新增 i18next 相关依赖 |
| `frontend/src/i18n/index.ts` | 新增 | i18next 初始化配置 |
| `frontend/src/i18n/locales/zh.json` | 新增 | 前端中文语言包 |
| `frontend/src/i18n/locales/en.json` | 新增 | 前端英文语言包 |
| `frontend/src/i18n/LanguageSwitcher.tsx` | 新增 | Header 语言切换组件 |
| `frontend/src/main.tsx` | 修改 | 引入 i18n 初始化 |
| `frontend/src/components/layout/AppLayout.tsx` | 修改 | 嵌入 LanguageSwitcher |
| `frontend/src/components/part-renderers/InteractivePermissionPart.tsx` | 修改 | 文本改用 t() |
| `frontend/src/components/part-renderers/InteractiveQuestionPart.tsx` | 修改 | 文本改用 t() |
| `frontend/src/components/chat/ChatPanel.tsx` | 修改 | 空状态文本改用 t() |
| `frontend/src/services/apiClient.ts` | 修改 | 添加 X-Lang Header |
| `crates/core/Cargo.toml` | 修改 | 新增 rust-i18n 依赖 |
| `crates/core/src/i18n/mod.rs` | 新增 | 后端 i18n 初始化 |
| `crates/core/src/i18n/locales/zh.toml` | 新增 | 后端中文语言包 |
| `crates/core/src/i18n/locales/en.toml` | 新增 | 后端英文语言包 |
| `crates/core/src/permission/permission.rs` | 修改 | 错误/提示文本改用 t!() |
| `crates/core/src/tools/mod.rs` | 修改 | 错误/提示文本改用 t!() |
| `crates/core/src/server/api/chat_api.rs` | 修改 | 读取 X-Lang Header |
| `crates/tui/Cargo.toml` | 修改 | 新增 rust-i18n 依赖 |
| `crates/tui/src/i18n/mod.rs` | 新增 | TUI i18n 初始化 |
| `crates/tui/src/i18n/locales/zh.toml` | 新增 | TUI 中文语言包 |
| `crates/tui/src/i18n/locales/en.toml` | 新增 | TUI 英文语言包 |
| `crates/tui/src/main.rs` | 修改 | 引入 i18n 初始化 |

---

### Task 1: 前端安装 i18next 依赖并初始化

**Files:**
- Modify: `frontend/package.json`
- Create: `frontend/src/i18n/index.ts`
- Create: `frontend/src/i18n/locales/zh.json`
- Create: `frontend/src/i18n/locales/en.json`
- Modify: `frontend/src/main.tsx`

- [ ] **Step 1: 安装依赖**

Run: `cd frontend && npm install i18next react-i18next i18next-browser-languagedetector`

- [ ] **Step 2: 创建 i18next 初始化配置**

```typescript
// frontend/src/i18n/index.ts
import i18n from 'i18next';
import { initReactI18next } from 'react-i18next';
import LanguageDetector from 'i18next-browser-languagedetector';
import zh from './locales/zh.json';
import en from './locales/en.json';

i18n
  .use(LanguageDetector)
  .use(initReactI18next)
  .init({
    resources: {
      zh: { translation: zh },
      en: { translation: en },
    },
    fallbackLng: 'en',
    interpolation: { escapeValue: false },
    detection: {
      order: ['localStorage', 'navigator', 'htmlTag'],
      caches: ['localStorage'],
    },
  });

export default i18n;
```

- [ ] **Step 3: 创建初始语言文件（第一阶段键）**

`frontend/src/i18n/locales/zh.json`:
```json
{
  "permission.toolLabel": "工具",
  "permission.riskLabel": "风险等级",
  "permission.reasonLabel": "原因",
  "permission.approve": "确认执行",
  "permission.reject": "拒绝",
  "permission.approved": "已确认执行",
  "permission.rejected": "已拒绝",
  "question.answered": "已回答",
  "question.customPlaceholder": "自定义回答...",
  "question.sendButton": "发送",
  "chat.thinking": "Thinking...",
  "chat.emptyTitle": "Welcome to fi-code",
  "chat.emptySubtitle": "Your AI-powered coding assistant",
  "chat.emptyHint": "Start a conversation or use /commands",
  "error.permissionDenied": "权限被拒绝",
  "error.permissionTimeout": "权限确认超时 (30s)",
  "error.questionTimeout": "问题响应超时 (60s)",
  "error.channelClosed": "响应通道已关闭"
}
```

`frontend/src/i18n/locales/en.json`:
```json
{
  "permission.toolLabel": "Tool",
  "permission.riskLabel": "Risk Level",
  "permission.reasonLabel": "Reason",
  "permission.approve": "Approve",
  "permission.reject": "Reject",
  "permission.approved": "✓ Approved",
  "permission.rejected": "✗ Rejected",
  "question.answered": "✓ Answered: {{answer}}",
  "question.customPlaceholder": "Custom answer...",
  "question.sendButton": "Send",
  "chat.thinking": "Thinking...",
  "chat.emptyTitle": "Welcome to fi-code",
  "chat.emptySubtitle": "Your AI-powered coding assistant",
  "chat.emptyHint": "Start a conversation or use /commands",
  "error.permissionDenied": "Permission denied",
  "error.permissionTimeout": "Permission confirmation timeout (30s)",
  "error.questionTimeout": "Question response timeout (60s)",
  "error.channelClosed": "Response channel closed"
}
```

- [ ] **Step 4: 在 main.tsx 中引入 i18n**

```typescript
// 在 main.tsx 顶部添加
import './i18n';
```

- [ ] **Step 5: Commit**

```bash
git add frontend/package.json frontend/src/i18n frontend/src/main.tsx
git commit -m "feat: add i18next framework and initial locale files"
```

---

### Task 2: 前端 LanguageSwitcher 组件 + 嵌入 Header

**Files:**
- Create: `frontend/src/i18n/LanguageSwitcher.tsx`
- Modify: `frontend/src/components/layout/AppLayout.tsx`

- [ ] **Step 1: 创建 LanguageSwitcher**

```typescript
import React from 'react';
import { useTranslation } from 'react-i18next';

export const LanguageSwitcher: React.FC = () => {
  const { i18n } = useTranslation();

  const toggleLanguage = () => {
    const next = i18n.language === 'zh' ? 'en' : 'zh';
    i18n.changeLanguage(next);
  };

  return (
    <button
      onClick={toggleLanguage}
      className="p-2 rounded-lg hover:bg-bg-overlay transition-colors text-sm text-text-muted"
      title={i18n.language === 'zh' ? 'Switch to English' : '切换到中文'}
    >
      {i18n.language === 'zh' ? '🇨🇳 中文' : '🇬🇧 EN'}
    </button>
  );
};
```

- [ ] **Step 2: 嵌入 AppLayout.tsx**

在 Header 区域（或合适位置）添加 `<LanguageSwitcher />`。

- [ ] **Step 3: Commit**

```bash
git add frontend/src/i18n/LanguageSwitcher.tsx frontend/src/components/layout/AppLayout.tsx
git commit -m "feat: add LanguageSwitcher component to header"
```

---

### Task 3: 前端核心组件文本迁移（第一阶段）

**Files:**
- Modify: `frontend/src/components/part-renderers/InteractivePermissionPart.tsx`
- Modify: `frontend/src/components/part-renderers/InteractiveQuestionPart.tsx`
- Modify: `frontend/src/components/chat/ChatPanel.tsx`
- Modify: `frontend/src/hooks/useChatStream.ts`

- [ ] **Step 1: InteractivePermissionPart 改用 t()**

```tsx
import { useTranslation } from 'react-i18next';

const { t } = useTranslation();

// 替换所有硬编码中文：
// "工具" → t('permission.toolLabel')
// "风险等级" → t('permission.riskLabel')
// "确认执行" → t('permission.approve')
// "拒绝" → t('permission.reject')
// "已确认执行" → t('permission.approved')
// "已拒绝" → t('permission.rejected')
```

- [ ] **Step 2: InteractiveQuestionPart 改用 t()**

```tsx
// "自定义回答..." → t('question.customPlaceholder')
// "发送" → t('question.sendButton')
// "已回答" → t('question.answered', { answer: part.answer })
```

- [ ] **Step 3: ChatPanel 空状态改用 t()**

```tsx
// "Welcome to fi-code" → t('chat.emptyTitle')
// "Your AI-powered coding assistant" → t('chat.emptySubtitle')
// "Start a conversation or use /commands" → t('chat.emptyHint')
// "Thinking..." → t('chat.thinking')
```

- [ ] **Step 4: Commit**

```bash
git add frontend/src/components frontend/src/hooks/useChatStream.ts
git commit -m "feat: migrate core component texts to i18n (phase 1)"
```

---

### Task 4: 后端 rust-i18n 初始化

**Files:**
- Modify: `crates/core/Cargo.toml`
- Create: `crates/core/src/i18n/mod.rs`
- Create: `crates/core/src/i18n/locales/zh.toml`
- Create: `crates/core/src/i18n/locales/en.toml`

- [ ] **Step 1: 添加 rust-i18n 依赖**

```toml
# crates/core/Cargo.toml [dependencies]
rust-i18n = "3"
```

- [ ] **Step 2: 创建后端 i18n 模块**

```rust
// crates/core/src/i18n/mod.rs
use std::sync::atomic::{AtomicBool, Ordering};

rust_i18n::i18n!("locales", fallback = "en");

static LANG_SET: AtomicBool = AtomicBool::new(false);

pub fn set_language(lang: &str) {
    rust_i18n::set_locale(lang);
    LANG_SET.store(true, Ordering::Relaxed);
}

pub fn current_language() -> String {
    if LANG_SET.load(Ordering::Relaxed) {
        rust_i18n::locale().to_string()
    } else {
        // 默认检测系统语言
        std::env::var("LANG")
            .unwrap_or_default()
            .split('.')
            .next()
            .unwrap_or("en")
            .split('_')
            .next()
            .unwrap_or("en")
            .to_string()
    }
}
```

- [ ] **Step 3: 创建后端语言文件**

`crates/core/src/i18n/locales/zh.toml`:
```toml
[error]
sudoDetected = "检测到 sudo 越权访问"
rmrfDetected = "检测到 rm -rf 危险操作"
bashInjection = "检测到潜在的 bash 注入攻击"
permissionDenied = "权限被拒绝：用户拒绝"
permissionTimeout = "权限确认超时 (30秒)"
questionTimeout = "问题响应超时 (60秒)"
channelClosed = "响应通道已关闭"

[tool]
bashHighRisk = "bash 工具需要用户确认"
writeHighRisk = "工具 {name} 可能修改文件，需要确认"
editHighRisk = "工具 {name} 可能修改文件，需要确认"
autoApproved = "工具 {name} 自动放行（低风险）"
```

`crates/core/src/i18n/locales/en.toml`:
```toml
[error]
sudoDetected = "sudo privilege escalation detected"
rmrfDetected = "rm -rf dangerous operation detected"
bashInjection = "potential bash injection attack detected"
permissionDenied = "Permission denied: user rejected"
permissionTimeout = "Permission confirmation timeout (30s)"
questionTimeout = "Question response timeout (60s)"
channelClosed = "Response channel closed"

[tool]
bashHighRisk = "tool bash requires user confirmation"
writeHighRisk = "tool {name} may modify files, requires confirmation"
editHighRisk = "tool {name} may modify files, requires confirmation"
autoApproved = "tool {name} auto-approved (low risk)"
```

- [ ] **Step 4: 在 core lib.rs 中注册 i18n 模块**

在 `crates/core/src/lib.rs` 中添加 `pub mod i18n;`。

- [ ] **Step 5: Commit**

```bash
git add crates/core/Cargo.toml crates/core/src/i18n crates/core/src/lib.rs
git commit -m "feat: add rust-i18n framework to backend"
```

---

### Task 5: 后端文本迁移（permission.rs + tools/mod.rs）

**Files:**
- Modify: `crates/core/src/permission/permission.rs`
- Modify: `crates/core/src/tools/mod.rs`

- [ ] **Step 1: permission.rs 文本迁移**

将所有硬编码的英文错误/提示文本替换为 `t!()` 调用：

```rust
// 替换前:
"sudo privilege escalation detected"
// 替换后:
t!("error.sudoDetected")

// 替换前:
"rm -rf dangerous operation detected"
// 替换后:
t!("error.rmrfDetected")

// 替换前:
"potential bash injection attack detected"
// 替换后:
t!("error.bashInjection")

// 替换前:
"tool bash requires user confirmation"
// 替换后:
t!("tool.bashHighRisk")

// 替换前:
format!("tool {} may modify files, requires confirmation", tool_name)
// 替换后:
t!("tool.writeHighRisk", name = tool_name)

// 替换前:
format!("tool {} auto-approved (low risk)", tool_name)
// 替换后:
t!("tool.autoApproved", name = tool_name)
```

- [ ] **Step 2: tools/mod.rs 错误消息迁移**

```rust
// "Permission denied: user rejected" → t!("error.permissionDenied")
// "Permission confirmation timeout (30s)" → t!("error.permissionTimeout")
// "Question response timeout (60s)" → t!("error.questionTimeout")
// "Question response channel closed" → t!("error.channelClosed")
// "Failed to send permission response" → t!("error.channelClosed")
// "Failed to send question response" → t!("error.channelClosed")
```

- [ ] **Step 3: 编译检查**

Run: `cargo check -p fi-code-core`

- [ ] **Step 4: 运行测试**

Run: `cargo test -p fi-code-core permission`
Expected: 全部通过（可能需要更新测试断言以匹配新的 t!() 输出）

- [ ] **Step 5: Commit**

```bash
git add crates/core/src/permission/permission.rs crates/core/src/tools/mod.rs
git commit -m "feat: migrate backend error messages to i18n"
```

---

### Task 6: 后端 X-Lang Header 支持

**Files:**
- Modify: `crates/core/src/server/api/chat_api.rs`
- Modify: `frontend/src/services/apiClient.ts`

- [ ] **Step 1: chat_api.rs 读取 X-Lang Header**

```rust
// 在 handle_chat_endpoint 中读取 Header
let lang = headers
    .get("X-Lang")
    .and_then(|v| v.to_str().ok())
    .unwrap_or("en");
crate::i18n::set_language(lang);
```

- [ ] **Step 2: apiClient.ts 添加 X-Lang Header**

```typescript
// 在 apiClient 的构造函数或请求方法中，读取当前 i18n 语言并添加到 Header
private getHeaders(): Record<string, string> {
  const lang = i18n.language || 'en';
  return {
    'Content-Type': 'application/json',
    'X-Lang': lang,
  };
}
```

- [ ] **Step 3: Commit**

```bash
git add crates/core/src/server/api/chat_api.rs frontend/src/services/apiClient.ts
git commit -m "feat: pass language preference via X-Lang header"
```

---

### Task 7: TUI rust-i18n 初始化

**Files:**
- Modify: `crates/tui/Cargo.toml`
- Create: `crates/tui/src/i18n/mod.rs`
- Create: `crates/tui/src/i18n/locales/zh.toml`
- Create: `crates/tui/src/i18n/locales/en.toml`
- Modify: `crates/tui/src/main.rs`

- [ ] **Step 1: 添加 rust-i18n 依赖**

```toml
# crates/tui/Cargo.toml [dependencies]
rust-i18n = "3"
```

- [ ] **Step 2: 创建 TUI i18n 模块和语言文件**

与 Task 4 类似，但语言文件放在 `crates/tui/src/i18n/locales/` 下，包含 TUI 特有的 UI 文本（导航、快捷键提示等）。

- [ ] **Step 3: main.rs 初始化语言**

```rust
// 在 main.rs 启动时检测 LANG 环境变量并设置
let lang = std::env::var("LANG")
    .unwrap_or_default()
    .split('.')
    .next()
    .unwrap_or("en")
    .split('_')
    .next()
    .unwrap_or("en");
i18n::set_language(lang);
```

- [ ] **Step 4: Commit**

```bash
git add crates/tui/Cargo.toml crates/tui/src/i18n crates/tui/src/main.rs
git commit -m "feat: add rust-i18n framework to TUI"
```

---

### Task 8: 编译与集成测试

**Files:**
- 所有已修改文件

- [ ] **Step 1: 前端 TypeScript 编译检查**

Run: `cd frontend && npx tsc --noEmit`
Expected: 无类型错误

- [ ] **Step 2: Rust 编译检查**

Run: `cargo check`
Expected: 无编译错误

- [ ] **Step 3: Rust 测试**

Run: `cargo test -p fi-code-core`
Expected: 全部通过（注意 pre-existing 失败的测试）

- [ ] **Step 4: 手动验证**

1. 启动后端和前端
2. 打开浏览器，观察 Header 右上角是否有语言切换按钮
3. 点击切换为 English，刷新页面，观察 PermissionAsk / QuestionAsk 文本是否变为英文
4. 发送触发 write 的消息，观察后端返回的错误/提示是否为英文
5. 切换回中文，验证中文显示正常

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: i18n internationalization for Web, TUI and backend"
```

---

## Self-Review Checklist

**1. Spec coverage:**
- ✅ Web i18next 框架 → Task 1
- ✅ LanguageSwitcher → Task 2
- ✅ 前端核心组件迁移 → Task 3
- ✅ 后端 rust-i18n → Task 4
- ✅ 后端文本迁移 → Task 5
- ✅ X-Lang Header → Task 6
- ✅ TUI rust-i18n → Task 7
- ✅ 编译与测试 → Task 8

**2. Placeholder scan:**
- ✅ 无 TBD/TODO
- ✅ 所有步骤包含实际代码

**3. Type consistency：**
- ✅ 键名规范在所有端一致
- ✅ rust-i18n 和 react-i18next 的插值语法一致（`{name}` / `{{name}}` 注意差异）

# Glob Tool Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现一个新的 `glob` 工具，支持使用 glob 模式搜索文件。

**Architecture:** 在现有的 `BasicTool` 结构中添加 `run_glob` 静态方法，新建 `GlobHandler` 实现 `ToolHandler` trait，并在工具注册表中注册。与现有工具风格保持一致。

**Tech Stack:** 
- Rust 语言
- `glob` crate（新增依赖）
- 现有项目结构和工具系统

---

### Task 1: 添加 glob 依赖到 Cargo.toml

**Files:**
- Modify: `Cargo.toml`

- [ ] **Step 1: 在 Cargo.toml 中添加 glob 依赖**

找到 `[dependencies]` 部分，添加：
```toml
glob = "0.3"
```

- [ ] **Step 2: 验证依赖添加正确**

运行: `cargo check`
Expected: 编译成功，无错误

- [ ] **Step 3: 提交**

```bash
git add Cargo.toml
git commit -m "feat: add glob dependency"
```

---

### Task 2: 在 BasicTool 中添加 run_glob 方法（TDD）

**Files:**
- Modify: `src/tools/basic_tools.rs`

- [ ] **Step 1: 先写测试用例（在 tests 模块）**

```rust
#[test]
fn test_run_glob() {
    ensure_workspace();
    let result = BasicTool::run_glob("**/Cargo.toml", None);
    assert!(result.is_ok());
    let output = result.unwrap();
    assert!(output.contains("Cargo.toml"));
}

#[test]
fn test_run_glob_no_matches() {
    ensure_workspace();
    let result = BasicTool::run_glob("**/nonexistent_file_1234.xyz", None);
    assert!(result.is_ok());
    assert_eq!(result.unwrap(), "No files found matching pattern");
}
```

- [ ] **Step 2: 运行测试确保失败**

运行: `cargo test --test test_run_glob`
Expected: 编译错误（函数未定义）

- [ ] **Step 3: 添加 use 语句**

在文件顶部的其他 use 语句后添加：
```rust
use glob::glob_with;
use glob::MatchOptions;
```

- [ ] **Step 4: 实现 run_glob 静态方法**

在 `BasicTool` impl 块中添加（在 `run_grep` 之后）：
```rust
pub fn run_glob(pattern: &str, dir: Option<&str>) -> Result<String, String> {
    let base = workspace_dir();
    let search_dir = match dir {
        Some(d) => {
            let safe_dir = Self::safe_path(d)?;
            safe_dir
        }
        None => base.clone(),
    };
    
    log_trace!("run_glob | pattern={} | dir={:?}", pattern, search_dir);
    
    let full_pattern = search_dir.join(pattern);
    let full_pattern_str = full_pattern.to_str().ok_or("Invalid pattern path")?;
    
    let options = MatchOptions {
        case_sensitive: true,
        require_literal_separator: false,
        require_literal_leading_dot: false,
    };
    
    let mut files = Vec::new();
    
    match glob_with(full_pattern_str, options) {
        Ok(paths) => {
            for entry in paths {
                match entry {
                    Ok(path) => {
                        if !path.is_file() {
                            continue;
                        }
                        let canonical = path.canonicalize().map_err(|e| format!("Error: {}", e))?;
                        if !canonical.starts_with(&base) {
                            continue;
                        }
                        let relative = canonical.strip_prefix(&base)
                            .map_err(|e| format!("Error: {}", e))?
                            .display()
                            .to_string();
                        files.push(relative);
                        
                        if files.len() >= 1000 {
                            files.push("... (too many matches)".to_string());
                            break;
                        }
                    }
                    Err(e) => {
                        log_trace!("glob error: {}", e);
                        continue;
                    }
                }
            }
        }
        Err(e) => {
            return Err(format!("Invalid pattern: {}", e));
        }
    }
    
    if files.is_empty() {
        Ok("No files found matching pattern".to_string())
    } else {
        let result = files.join("\n");
        Ok(result.chars().take(50000).collect())
    }
}
```

- [ ] **Step 5: 运行测试确保通过**

运行: `cargo test`
Expected: 所有测试通过，包括我们的新测试

- [ ] **Step 6: 提交**

```bash
git add src/tools/basic_tools.rs
git commit -m "feat: add run_glob method to BasicTool"
```

---

### Task 3: 创建 GlobHandler 并在工具注册表中注册

**Files:**
- Modify: `src/tools/mod.rs`

- [ ] **Step 1: 添加 GlobHandler 结构体**

在 `GrepHandler` 之后、`UseSkillHandler` 之前添加：
```rust
struct GlobHandler;

impl ToolHandler for GlobHandler {
    fn call(&self, _name: &str, params: ToolParams) -> Result<String, String> {
        let (pattern, dir) = match &params[..] {
            [ToolParameter::Json(v)] => {
                let pattern = get_json_param(v, "pattern");
                let dir = v.get("dir").and_then(|x| x.as_str());
                (pattern, dir)
            }
            [ToolParameter::String(p)] => (p.clone(), None),
            [ToolParameter::String(p), ToolParameter::String(d)] => (p.clone(), Some(d)),
            _ => ("".to_string(), None),
        };

        if pattern.is_empty() {
            return Err("Missing pattern parameter".to_string());
        }

        BasicTool::run_glob(&pattern, dir)
    }
}
```

- [ ] **Step 2: 在工具注册表中注册 glob 工具**

找到 `REGISTRY` 初始化的地方，在 `grep` 工具注册后、`use_skill` 工具注册前添加：
```rust
registry.register(
    "glob",
    "使用 glob 模式搜索文件，支持 *、**、?、[] 等模式",
    r#"{"type":"object","properties":{"pattern":{"type":"string","description":"Glob 模式，如 **/*.rs、src/**/*、*.md"},"dir":{"type":"string","description":"可选，搜索根目录，默认为当前工作目录"}},"required":["pattern"]}"#,
    Box::new(GlobHandler),
).expect("register glob tool failed");
```

- [ ] **Step 3: 运行编译确保正确**

运行: `cargo build`
Expected: 编译成功

- [ ] **Step 4: 运行所有测试**

运行: `cargo test`
Expected: 所有 110+ 个测试通过

- [ ] **Step 5: 添加工具的集成测试**

在 `mod.rs` 的 `tests` 模块中添加（在 `test_tool_call_grep` 之后）：
```rust
#[tokio::test]
async fn test_tool_call_glob() {
    let mut input = HashMap::new();
    input.insert("pattern".to_string(), serde_json::json!("**/Cargo.toml"));
    let result = tool_call("glob", &input).await;
    assert!(result.is_ok());
    let output = result.unwrap();
    assert!(output.contains("Cargo.toml"));
}
```

- [ ] **Step 6: 运行新的集成测试**

运行: `cargo test`
Expected: 新测试通过

- [ ] **Step 7: 提交**

```bash
git add src/tools/mod.rs
git commit -m "feat: add glob tool"
```

---

### Task 4: 更新 list_tools 测试并最终验证

**Files:**
- Modify: `src/tools/mod.rs`（测试部分）

- [ ] **Step 1: 更新 test_list_tools 测试**

找到 `test_list_tools` 函数，添加对 "glob" 的检查：
```rust
#[test]
fn test_list_tools() {
    let list = REGISTRY.list_tools().unwrap();
    assert!(list.contains("bash"), "registry should contain bash tool");
    assert!(list.contains("read"), "registry should contain read tool");
    assert!(list.contains("write"), "registry should contain write tool");
    assert!(list.contains("edit"), "registry should contain edit tool");
    assert!(
        list.contains("web_fetch"),
        "registry should contain web_fetch tool"
    );
    assert!(list.contains("grep"), "registry should contain grep tool");
    assert!(list.contains("glob"), "registry should contain glob tool"); // 新增
}
```

- [ ] **Step 2: 运行所有测试确保通过**

运行: `cargo test`
Expected: 所有测试通过

- [ ] **Step 3: 测试整个工具系统**

运行: `cargo build`
Expected: 编译成功，无错误

- [ ] **Step 4: 最终提交**

```bash
git add src/tools/mod.rs
git commit -m "test: add glob to list_tools test"
```

[package]
name = "fi-code-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "fi-code-cli"
path = "src/main.rs"

[dependencies]
fi-code-core = { path = "../core" }
tokio = { version = "1.35", features = ["full"] }
anyhow = "1.0"

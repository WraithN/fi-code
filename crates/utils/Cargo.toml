[package]
name = "fi-code-utils"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.35", features = ["process", "time", "rt-multi-thread"] }
anyhow = "1.0"
tempfile = "3.10"
serde_json = "1.0"
serde = { version = "1.0", features = ["derive"] }

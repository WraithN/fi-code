[package]
name = "fi-code-tui"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "fi-code-tui"
path = "src/main.rs"

[dependencies]
fi-code-core = { path = "../core" }
tokio = { version = "1.35", features = ["full"] }
anyhow = "1.0"
clap = { version = "4.4", features = ["derive"] }
